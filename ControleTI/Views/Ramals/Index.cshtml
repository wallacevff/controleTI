@model IEnumerable<ControleTI.Models.ViewModels.RamalViewModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ramal.PontoId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ramal.VoicePanelPonto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ramal.RamalNro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ramal.PontoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ramal.VoicePanelPonto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ramal.RamalNro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Ramal.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Ramal.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Ramal.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
